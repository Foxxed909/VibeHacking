# Python
.venv/
__pycache__/
*.pyc
*.pyo

# VibeHacking artifacts (regenerated each run)
backups/
logs/
vibe_session.json
VIBE_DASHBOARD.html
authorized_targets.txt

# Browser-assisted session captures — these ARE live sessions (secrets)
session_auth.json
*session_auth*.json
*.session.json
helpers/browser/node_modules/

# Tooling / transient
.playwright-mcp/
.DS_Store
Thumbs.db
*.tmp

# Batch launchers (generated per-tool)
*.bat

# Compiled Go binary — build on demand with `go build` in TOOLS/maelstrom/
TOOLS/maelstrom/maelstrom.exe

# Generated pentest reports & session artifacts
reports/
vb/__pycache__/

# Compiled Go binary (build on demand)
TOOLS/maelstrom/maelstrom

# NovaChat runtime-generated assets
testapp/public/

# Engagement tool output (may contain live responses)
engagements/output/
